import { DialogConfig } from "@ngneat/dialog";


export const dialogConfig: DialogConfig = {
  closeButton: true,
  enableClose: true,
  overflow: true,
  size: "md",
  windowClass: "custom-dialog-class",
  
} as DialogConfig