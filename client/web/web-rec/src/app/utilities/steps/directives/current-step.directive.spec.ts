import { CurrentStepDirective } from './current-step.directive';

describe('CurrentStepDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrentStepDirective();
    expect(directive).toBeTruthy();
  });
});
