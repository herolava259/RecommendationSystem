import { StepNavigationDirective } from './step-navigation.directive';

describe('StepNavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new StepNavigationDirective();
    expect(directive).toBeTruthy();
  });
});
